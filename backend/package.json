{
	"name": "backend",
	"version": "1.0.0",
	"description": "LinkNest Backend API",
	"main": "server.js",
	"scripts": {
		"dev": "NODE_ENV=development nodemon server.js",
		"start": "NODE_ENV=production node server.js",
		"build": "npm install && npm install --prefix ../frontend && npm run build --prefix ../frontend"
	},
	"type": "module",
	"engines": {
		"node": ">=18.0.0",
		"npm": ">=8.0.0"
	},
	"keywords": ["social-media", "api", "nodejs", "express"],
	"author": "LinkNest Team",
	"license": "MIT",
	"dependencies": {
		"bcryptjs": "^2.4.3",
		"cloudinary": "^1.40.0",
		"cookie-parser": "^1.4.6",
		"cron": "^3.1.6",
		"dotenv": "^16.3.1",
		"express": "^4.18.2",
		"jsonwebtoken": "^9.0.1",
		"mongoose": "^7.4.0",
		"socket.io": "^4.7.2"
	},
	"devDependencies": {
		"nodemon": "^3.0.1"
	}
}
